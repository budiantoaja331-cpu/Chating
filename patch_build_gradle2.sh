sed -i 's/create("debugConfig") {/create("release") {\n      val storeFilePath = keystoreProperties.getProperty("storeFile")\n      if (storeFilePath != null) {\n        storeFile = file(storeFilePath)\n        storePassword = keystoreProperties.getProperty("storePassword")\n        keyAlias = keystoreProperties.getProperty("keyAlias")\n        keyPassword = keystoreProperties.getProperty("keyPassword")\n      }\n    }\n    create("debugConfig") {/' app/build.gradle.kts
sed -i 's/\/\/ signingConfig = signingConfigs.getByName("release")/signingConfig = signingConfigs.getByName("release")/' app/build.gradle.kts
